<%#
  web/views/partials/tier-badge.ejs
  Selo "HUNTER" (Figma: "user.icon") — pedido do dono pra marcar
  visualmente seções exclusivas do plano Caçador (nome em inglês do tier,
  mesma convenção de hero.ejs:572 — Free/Tracker/Hunter). Estático, sem
  parâmetro: só existe esse tier badge hoje (Caçador); se um dia precisar
  marcar outro tier, adiciona um `tier` opcional aqui em vez de duplicar
  o componente.

  Uso: include( partials/tier-badge )
%>
<span class="tier-badge">
    <span class="tier-badge-text">HUNTER</span>
</span>
