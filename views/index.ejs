<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard | <%= bot.user.username %></title>
    
    <!-- Importação das Fontes -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Tilt+Warp&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-main: #1F1D20;
            --bg-card: #2A2A2A;
            --bg-inner: #1F1D20;
            --text-primary: #F8DCC0;
            --text-secondary: #A89986;
            --accent: #DCA15E;
            --surface: #803E30;
            --surface-hover: #4B2427;
            --stroke: #5A1811;
            --shadow-black: rgba(0, 0, 0, 0.5);
            
            /* Definição das fontes nas variáveis */
            --font-head: 'Tilt Warp', cursive;
            --font-body: 'Poppins', sans-serif;
        }

        body {
            font-family: var(--font-body); /* Poppins como padrão */
            background-color: var(--bg-main);
            color: var(--text-primary);
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .login-card {
            background-color: var(--bg-inner);
            width: 100%;
            max-width: 450px;
            padding: 50px 30px;
            border-radius: 40px;
            box-shadow: 20px 20px 60px #131214, -20px -20px 60px #2b282c;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 3px solid var(--stroke);
            object-fit: cover;
            margin-bottom: 25px;
            box-shadow: 0 10px 20px var(--shadow-black);
        }

        /* Aplicação da Tilt Warp nos cabeçalhos */
        .nickname {
            font-family: var(--font-head);
            font-size: 2.2rem;
            color: var(--text-primary);
            text-transform: uppercase;
            margin-bottom: 5px;
            letter-spacing: 1px;
        }

        .role-badge {
            font-family: var(--font-head); /* Badge também fica bem com a Tilt */
            background-color: var(--surface-hover);
            color: var(--text-primary);
            padding: 6px 20px;
            border-radius: 12px;
            font-size: 0.9rem;
            display: inline-block;
            border: 2px solid var(--stroke);
            margin-bottom: 30px;
        }

        .guilds-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin-top: 20px;
        }

        .guild-square {
            aspect-ratio: 1 / 1;
            background-color: var(--bg-card);
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 1px solid transparent;
            text-decoration: none;
            overflow: hidden;
        }

        .guild-square:hover {
            transform: scale(1.05);
            border-color: var(--accent);
            background-color: var(--surface-hover);
        }

        .guild-initials {
            font-family: var(--font-head);
            color: var(--text-secondary);
            font-size: 1.4rem;
        }

        .btn-primary {
            font-family: var(--font-head);
            background-color: var(--surface);
            color: var(--text-primary);
            padding: 14px 35px;
            border-radius: 14px;
            text-decoration: none;
            display: inline-block;
            transition: 0.3s;
            border: 1px solid var(--stroke);
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            text-transform: uppercase;
            font-size: 1.1rem;
        }

        .btn-primary:hover {
            background-color: var(--surface-hover);
            box-shadow: 0 0 20px rgba(128, 62, 48, 0.4);
        }

        .footer-text {
            font-family: var(--font-body);
            font-weight: 300;
            position: absolute;
            bottom: 20px;
            color: var(--text-secondary);
            font-size: 0.75rem;
            letter-spacing: 2px;
        }

        .logout {
            font-family: var(--font-body);
            margin-top: 20px;
            display: block;
            color: var(--text-secondary);
            font-size: 0.85rem;
            text-decoration: none;
            transition: 0.2s;
        }
        .logout:hover { color: var(--accent); }
    </style>
</head>
<body>

    <div class="login-card">
        <% if (user) { %>
            <img class="avatar" src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" onerror="this.src='https://discord.com/assets/f78426a064bc9dd24847519294b081dd.png'">

            <div class="nickname"><%= user.username %></div>
            
            <div class="role-badge">
                <%= isAdmin ? 'ADMINISTRADOR' : 'STAFF' %>
            </div>

            <% if (isAdmin) { %>
                <div class="guilds-grid">
                    <% guilds.slice(0, 6).forEach(guild => { %>
                        <a href="/home/<%= guild.id %>" class="guild-square">
                            <% if (guild.icon) { %>
                                <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" style="width:100%; height:100%; object-fit:cover;">
                            <% } else { %>
                                <span class="guild-initials"><%= guild.name.split(' ').map(i => i[0]).join('') %></span>
                            <% } %>
                        </a>
                    <% }); %>
                </div>
                <br>
                <a href="/punicoes" class="btn-primary" style="margin-top: 15px;">Histórico</a>
            <% } %>

            <a href="/logout" class="logout">Sair da conta</a>

        <% } else { %>
            <div class="nickname" style="margin-bottom: 20px;">ROBIN V3</div>
            <p style="color: var(--text-secondary); font-weight: 300;">Faça login para gerenciar seus servidores e punições.</p>
            <a href="/login" class="btn-primary">Entrar com Discord</a>
        <% } %>
    </div>

    <div class="footer-text">BY: KNUST SEIER VI</div>

</body>
</html>